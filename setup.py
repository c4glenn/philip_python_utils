from setuptools import setup, find_packages

setup(
    name="philip_utils",
    version='0.1.0',
    author='Philip Smith',
    author_email='philip@randomsmiths.com',
    packages=find_packages(),
    install_requires=[
        'enlighten'
    ]
)